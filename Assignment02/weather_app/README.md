# OpenWeather App

Simple Python app that fetches weather using OpenWeatherMap API.

## Steps to run:

1. Create venv
   python -m venv venv
   venv\Scripts\activate

2. Install requirements
   pip install -r requirements.txt

3. Add your API key in app/config.py

4. Run app
   python app/main.py
